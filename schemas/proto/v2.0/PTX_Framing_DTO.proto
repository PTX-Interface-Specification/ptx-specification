syntax = "proto3";

package io.ptx.v2; // tentative

import "google/protobuf/timestamp.proto";

import "chrusty/protoc-gen-jsonschema/options.proto";

option java_multiple_files = true;

message PtxHeader {
    google.protobuf.Timestamp timestamp = 1 [(protoc.gen.jsonschema.field_options).required = true]; // [RFC 3339, cf. §5.2]
    string version = 2 [(protoc.gen.jsonschema.field_options).required = true]; // current version = 2.0.0
} // the header itself is not versioned -- only compatible extensions!!

